<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.atula.doanapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.login">
        <activity android:name=".ui.SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.user.activity.ThemThanhVienActivity"
            android:theme="@style/Theme.login" />
        <activity
            android:name=".ui.user.activity.XoaThanhVienActivity"
            android:theme="@style/Theme.login" />
        <activity
            android:name=".ui.user.activity.ChangePassActivity"
            android:theme="@style/Theme.login" />
        <activity
            android:name=".ui.user.activity.UpdateUserActivity"
            android:theme="@style/Theme.login" />
        <activity android:name=".ui.ad.activity.GiaoVienActivity" />
        <activity android:name=".ui.ad.activity.CreateDoanActivity" />
        <activity
            android:name=".ui.ad.activity.AdDetailActivity"
            android:theme="@style/Theme.login" />
        <activity android:name=".ui.ad.activity.UpdateDoanActivity" />
        <activity
            android:name=".ui.ad.activity.AdmindActivity"
            android:theme="@style/Theme.login" />
        <activity
            android:name=".ui.LoginActivity"
            android:theme="@style/Theme.login">

        </activity>
        <activity
            android:name=".ui.user.activity.MainActivity"
            android:theme="@style/Theme.user" />
    </application>

</manifest>